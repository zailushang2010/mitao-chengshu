# 安全说明

## 支持的版本

当前维护主线见 `Cargo.toml` 中的 `version`（发布标签以 GitHub Releases 为准）。

## 报告漏洞

若发现可被利用的安全问题（例如任意路径读写、意外执行外部程序等），请 **不要** 在公开 Issue 中详述利用方式。

请通过 GitHub 仓库的 **Security Advisories** 私密报告，或联系维护者（以仓库 About / 主页信息为准）。

我们会尽快确认并在修复后公开致谢（如你愿意具名）。

## 使用注意

- 配置与历史文件保存在 exe 同目录；请勿在不可信目录运行并写入敏感路径列表。  
- 本工具会启动用户指定的 PotPlayer 并传递本地文件路径；请确保片库路径可信。  
